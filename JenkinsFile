pipeline {
    agent { docker { image 'python:3.5.1' } }
    stages {
        stage('build') {
            steps {
                sh 'python main.py 4'
                sh 'pytest test.py'
            }
        }
    }
}